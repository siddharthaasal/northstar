# PostgreSQL connection string (Neon in production).
# Local example: postgres://postgres:postgres@localhost:5432/northstar
DATABASE_URL=

# Auth.js — generate with: npx auth secret
AUTH_SECRET=

# Add provider credentials as you enable them in src/auth.ts, e.g.:
# AUTH_GITHUB_ID=
# AUTH_GITHUB_SECRET=
